<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="utf-8" />
<meta name="viewport" content="width=device-width,initial-scale=1" />
<title > ᵤₜₖₐᵣₛₕ ❤ ₐₐyᵤₛₕᵢ — ₐ ₗᵢₜₜₗₑ ₛᵤᵣₚᵣᵢₛₑ</title>
<style>
    title{
        font-style: italic;
    }
  :root{
    --bg:#0f172a; --card:#0b1220; --accent:#ff4d6d; --muted:#99a3b5;
  }
  *{box-sizing:border-box}
  body{
    margin:0; font-family:Inter,system-ui,Segoe UI,Roboto,"Helvetica Neue",Arial;
    background: radial-gradient(circle at 10% 10%, #10203a 0%, #071022 60%);
    color:#e6eef8; display:flex; align-items:center; justify-content:center;
    min-height:100vh; padding:20px;
  }
  .card{
    width:100%; max-width:820px; background:linear-gradient(180deg, rgba(255,255,255,0.03), rgba(0,0,0,0.12));
    border-radius:14px; padding:28px; box-shadow:0 10px 30px rgba(2,6,23,0.6);
    position:relative; overflow:hidden;
  }
  header{display:flex; align-items:center; gap:14px}
  .logo{
    width:72px; height:72px; border-radius:12px; display:grid;place-items:center;
    background:linear-gradient(135deg, #ff9aa2, #ff4d6d); font-size:32px; font-weight:700;
    color:white;
  }
  h1{margin:0; font-size:20px}
  p.lead{margin:6px 0 18px; color:var(--muted)}

  .row{display:flex; gap:18px; align-items:flex-start}
  .left{flex:1}
  .right{width:260px; background:rgba(255,255,255,0.02); padding:14px; border-radius:10px}

  .heart{
    font-size:56px; cursor:pointer; user-select:none; transform-origin:center;
    transition:transform .18s ease;
    filter: drop-shadow(0 6px 18px rgba(255,77,109,0.25));
  }
  .heart.beat{ animation:beat .6s ease; }
  @keyframes beat{
    0%{ transform:scale(1) }
    30%{ transform:scale(1.28) }
    60%{ transform:scale(0.95) }
    100%{ transform:scale(1) }
  }

  label{display:block; font-size:13px; color:var(--muted); margin-bottom:6px}
  input[type="text"]{width:100%; padding:10px; border-radius:8px; background:transparent; border:1px solid rgba(255,255,255,0.06); color:inherit}
  button.cta{
    margin-top:12px; width:100%; padding:10px; border-radius:8px; border:0;
    background:linear-gradient(90deg,#ff7b94,#ff4d6d); color:white; font-weight:600; cursor:pointer;
    box-shadow:0 8px 18px rgba(255,77,109,0.14);
  }

  .message{ margin-top:12px; padding:10px; border-radius:8px; background:rgba(255,255,255,0.02); color:var(--muted) }

  /* falling emojis */
  .emoji {
    position:fixed; left:0; top:-50px; font-size:22px; pointer-events:none;
    animation:fall linear forwards;
  }
  @keyframes fall{
    to{ transform: translateY(120vh); opacity:0.02; }
  }

  footer{ margin-top:18px; font-size:13px; color:var(--muted)}
</style>
</head>
<body>
  <div class="card" id="card">
    <header>
      <div class="logo">L♡</div>
      <div>
        <h1> 🆄🆃🅺🅰🆁🆂🅷 " Little Surprise</h1>
        <p class="lead">For <strong>🆄🆃🅺🅰🆁🆂🅷</strong> — click the heart, type your name, and get a message ✨</p>
      </div>
    </header>

    <div class="row" style="margin-top:18px">
      <div class="left">
        <div style="display:flex;gap:14px;align-items:center">
          <div id="heart" class="heart" title="Click me!">💖</div>
          <div>
            <div style="font-size:16px;font-weight:700">Click the heart</div>
            <div style="color:var(--muted);font-size:13px">Every click makes it beat and throws some hearts ✨</div>
          </div>
        </div>

        <div style="margin-top:16px">
          <label for="name">Type your name (try: 🆄🆃🅺🅰🆁🆂🅷)</label>
          <input id="name" type="text" placeholder="Your name...">
          <button id="sayHi" class="cta">Say Hello →</button>
          <div id="greet" class="message">Waiting for your name...</div>
        </div>
      </div>

      <aside class="right">
        <div style="font-weight:700">Quick notes</div>
        <div style="margin-top:8px; color:var(--muted); font-size:13px">
          • Click the heart many times for fun.<br>
          • Type your name and press "Say Hello" to get a personalised message.<br>
          • I made this so you can copy, edit, and learn — change colours, text, everything!
        </div>
      </aside>
    </div>

    <footer>Made with care by Ritesh 💙 — Try changing the emoji or styles!</footer>
  </div>

<script>
  // Heart click: beat + spawn floating emojis
  const heart = document.getElementById('heart');
  heart.addEventListener('click', () => {
    heart.classList.remove('beat');
    // restart animation
    void heart.offsetWidth;
    heart.classList.add('beat');
    spawnEmojis();
  });

  // spawn random falling emojis (hearts & stars)
  function spawnEmojis(){
    const count = 8;
    const icons = ['💖','💘','💕','✨','🌟','💫'];
    for(let i=0;i<count;i++){
      const el = document.createElement('div');
      el.className = 'emoji';
      el.style.left = (Math.random()*100)+'vw';
      el.style.fontSize = (14 + Math.random()*28) + 'px';
      el.style.opacity = 0.9;
      el.style.animationDuration = (3 + Math.random()*4) + 's';
      el.textContent = icons[Math.floor(Math.random()*icons.length)];
      document.body.appendChild(el);
      // remove after animation
      setTimeout(()=> document.body.removeChild(el), 8000);
    }
  }

  // Greeting with small "typing" feel
  const sayHi = document.getElementById('sayHi');
  const nameInput = document.getElementById('name');
  const greet = document.getElementById('greet');

  sayHi.addEventListener('click', () => {
    const name = (nameInput.value || 'Ritesh').trim();
    const messages = [
      Hi ${name}! Leva says: Keep smiling 😊,
      ${name}, coding + coffee = 💪. You got this!,
      Hey ${name}! Nice to see you here — let's build cool things.,
      ${name}, whenever you're stuck, Leva will help — promise!
    ];
    // pick random message
    const msg = messages[Math.floor(Math.random()*messages.length)];
    animateText(greet, msg);
    // also spawn a joyful burst
    spawnEmojis();
  });

  // simple text-typing animation
  function animateText(target, text){
    target.textContent = '';
    let i = 0;
    const speed = 25;
    const timer = setInterval(()=>{
      target.textContent += text[i++] || '';
      if(i > text.length) clearInterval(timer);
    }, speed);
  }

  // allow Enter to trigger greeting
  nameInput.addEventListener('keydown', (e)=>{
    if(e.key === 'Enter') sayHi.click();
  });
</script>
</body>
</html>